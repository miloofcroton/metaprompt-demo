---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up Vite + React + TypeScript project with React Router and shell/page architecture.

Purpose: Establish the foundational structure that all features will build on — routing, layout patterns, and component organization.
Output: Working React app with shell component, navigation, and welcome page at root route.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Constraints from PROJECT.md:**
- Tech stack: React, Vite, React Router, TypeScript (no plain JS)
- No persistence needed
- Functional styling sufficient (no CSS frameworks required)

**Architecture pattern:**
Shell component wraps all pages with navigation. Page content renders inside shell via React Router outlet.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project</name>
  <files>package.json, tsconfig.json, vite.config.ts, src/main.tsx, src/App.tsx, index.html</files>
  <action>
    Run `npm create vite@latest . -- --template react-ts` to scaffold project in current directory.
    Install dependencies with `npm install`.
    Install React Router: `npm install react-router-dom`.
    Verify project runs with `npm run dev`.
  </action>
  <verify>npm run dev starts server without errors, localhost shows Vite + React default page</verify>
  <done>Project scaffolded, dependencies installed, dev server runs</done>
</task>

<task type="auto">
  <name>Task 2: Create shell/page architecture with React Router</name>
  <files>src/App.tsx, src/components/Shell.tsx, src/pages/WelcomePage.tsx, src/pages/TodoPage.tsx</files>
  <action>
    Create Shell component (src/components/Shell.tsx):
    - Navigation bar at top with links to "/" and "/todo"
    - Use React Router's `<Outlet />` to render child routes
    - Simple functional styling (no CSS framework)

    Create placeholder pages:
    - WelcomePage.tsx: Basic component returning placeholder text
    - TodoPage.tsx: Basic component returning placeholder text (will be implemented in Phase 2)

    Update App.tsx:
    - Set up BrowserRouter with routes
    - Shell as parent route with Outlet
    - "/" renders WelcomePage
    - "/todo" renders TodoPage
    - Use createBrowserRouter and RouterProvider pattern (React Router v6)
  </action>
  <verify>npm run dev shows app with navigation, clicking links changes routes</verify>
  <done>Shell renders with nav bar, both routes accessible, Outlet displays correct page component</done>
</task>

<task type="auto">
  <name>Task 3: Implement Welcome page with link to todo app</name>
  <files>src/pages/WelcomePage.tsx</files>
  <action>
    Update WelcomePage to include:
    - Welcome heading
    - Brief description of the app
    - Link to "/todo" page (use React Router's Link component)

    Keep styling minimal and functional.
  </action>
  <verify>Welcome page displays heading, description, and clickable link that navigates to /todo</verify>
  <done>Welcome page complete with working navigation link to todo page</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts without errors
- [ ] Navigation between "/" and "/todo" works
- [ ] Shell component visible on both pages
- [ ] Welcome page shows link to todo app
- [ ] No TypeScript errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Project structure follows shell/page pattern
- Both routes render correctly within shell
- Welcome page has working link to todo
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`:

# Phase 1 Plan 01: Foundation Summary

**[Substantive one-liner describing what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]
- [Key outcome 3]

## Files Created/Modified

- `path/to/file.ts` - Description
- `path/to/another.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 1 complete, ready for Phase 2: Todo Feature
</output>
