---
phase: 02-todo-feature
plan: 01
type: execute
---

<objective>
Implement functional todo list with add, toggle complete, and delete functionality.

Purpose: Complete the core feature of the app — a working todo list with basic CRUD operations in React state.
Output: Functional TodoPage with todo list that supports adding, toggling completion, and deleting items.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

**Prior decisions affecting this phase:**
- Phase 1: Used createBrowserRouter pattern (React Router v6)
- TodoPage placeholder already exists at src/pages/TodoPage.tsx

**Constraints from PROJECT.md:**
- No persistence (state lives in memory only)
- No categories, priorities, due dates — just text items with done/not-done
- Functional styling sufficient (no CSS framework)

**Existing files to modify:**
@src/pages/TodoPage.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Todo type and state management</name>
  <files>src/pages/TodoPage.tsx</files>
  <action>
    Define Todo type with:
    - id: number (use Date.now() for unique IDs)
    - text: string
    - completed: boolean

    Set up state in TodoPage:
    - todos: Todo[] using useState
    - newTodoText: string for input field

    Create handler functions:
    - addTodo: creates new todo from newTodoText, clears input
    - toggleTodo: toggles completed status by id
    - deleteTodo: removes todo by id
  </action>
  <verify>TypeScript compiles without errors (npm run build)</verify>
  <done>Todo type defined, state and handlers created, no TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Implement TodoPage UI with add, toggle, delete</name>
  <files>src/pages/TodoPage.tsx</files>
  <action>
    Build TodoPage UI:

    1. Input section:
       - Text input bound to newTodoText
       - Add button that calls addTodo
       - Handle Enter key to add todo

    2. Todo list:
       - Map over todos array
       - Each item shows:
         - Checkbox bound to completed, onChange calls toggleTodo
         - Text (with strikethrough style when completed)
         - Delete button that calls deleteTodo

    3. Styling:
       - Keep minimal and functional (inline styles or simple CSS)
       - Strikethrough for completed items
       - Basic layout (input at top, list below)

    Note: State resets on page refresh — this is expected per PROJECT.md constraints.
  </action>
  <verify>
    npm run dev shows todo page at /todo with:
    - Input field and add button
    - Can type and add new todos
    - Todos appear in list with checkbox
    - Clicking checkbox toggles strikethrough
    - Delete button removes todo from list
  </verify>
  <done>Full todo CRUD working: add, toggle complete, delete. UI functional and usable.</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run build` succeeds without errors
- [ ] `npm run dev` starts without errors
- [ ] Can add new todos via input + button
- [ ] Can toggle todo completion via checkbox
- [ ] Completed todos show strikethrough
- [ ] Can delete todos via delete button
- [ ] No TypeScript errors
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Todo list is fully functional (add, toggle, delete)
- UI is usable and clear
- State management works correctly
</success_criteria>

<output>
After completion, create `.planning/phases/02-todo-feature/02-01-SUMMARY.md`:

# Phase 2 Plan 01: Todo Feature Summary

**[Substantive one-liner describing what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 2 complete — milestone complete, all features implemented.
</output>
